{
    "name": "voyagerss",
    "version": "1.0.0",
    "private": true,
    "description": "Voyagerss monorepo - backend & frontend",
    "scripts": {
        "setup": "npm install --prefix backend && npm install --prefix frontend",
        "predev": "npx kill-port 9002 9003",
        "dev": "concurrently -n be,fe -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:ai": "npx pnpm@9 install --dir ai-platform",
        "dev:ai": "npx pnpm@9 --dir ai-platform dev",
        "build:ai": "npx pnpm@9 --dir ai-platform build",
        "test:ai": "npx pnpm@9 --dir ai-platform test",
        "lint:ai": "npx pnpm@9 --dir ai-platform lint",
        "build:backend": "npm run build --prefix backend",
        "build:frontend": "npm run build --prefix frontend",
        "build:all": "npm run build:backend && npm run build:frontend",
        "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
        "format": "npm run format --prefix backend && npm run format --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.1.2",
        "kill-port": "^2.0.1"
    }
}