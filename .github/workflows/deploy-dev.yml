# Deprecated: use deploy-production.yml
# Kept with workflow_dispatch only to avoid duplicate auto-deploys on main.

name: Deploy Backend (GHCR) [deprecated]

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Use deploy-production.yml
        run: |
          echo "This workflow is deprecated. Use 'Deploy Production (GHCR)' instead." >&2
          exit 1
