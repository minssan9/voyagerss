# Production domains: FE https://voyagerss.com | BE https://api.voyagerss.com
# Compose variables for deploy/docker-compose.prod.yml (not backend secrets).
# On the droplet, set these when pulling images (CI sets IMAGE_TAG automatically).
GITHUB_OWNER=your-github-owner
IMAGE_TAG=latest
